# Independent reproduction metadata.
# Keep values single-line so scripts/models.js can parse this file without a full YAML dependency.

- slug: cpg-spmt
  status: success
  language: Python
  environment: Python environment with required scientific stack
  primary_blocker: none
  likely_next_step: none
  runtime: Python 3.9.23-compatible environment
  dependencies: `numpy` 1.26.4; `scipy` 1.11.4; `matplotlib` 3.9.4; `pandas` 1.5.3; `scikit-learn` 1.6.1; `torch` 2.2.2
  notes: Use any environment that provides the listed scientific Python dependencies.

- slug: pybamm
  status: success
  language: Python
  environment: Python 3.11 environment from recipe
  environment_file: REPRODUCTIONS/environments/pybamm/environment.yml
  primary_blocker: none
  likely_next_step: none
  runtime: Python 3.11.15 environment from recipe
  dependencies: `pybamm` 26.4.1; `numpy` 2.4.4; `scipy` 1.17.1; `matplotlib` 3.10.9; `pandas` 3.0.2; `casadi` 3.7.2
  notes: Recommended Python baseline for SPM/SPMe/DFN.

- slug: battmo
  status: success
  language: MATLAB
  environment: MATLAB R2021b
  primary_blocker: submodules required; Octave remains blocked on MATLAB-only `immutable` class-property syntax
  likely_next_step: keep MATLAB as the supported smoke path and document submodule initialization
  runtime: MATLAB R2021b
  dependencies: Upstream submodules required
  notes: Octave remains blocked on MATLAB-only `immutable` class-property syntax.

- slug: p2d-li-ion-battery-decaluwe
  status: unreproduced
  language: Python
  environment: Docker `micromamba` Python 3.8 on `linux/amd64`
  primary_blocker: final attempt with `algvar` still fails IDA convergence at `t=0`; full-cell residual appears incomplete
  likely_next_step: Audit residual equations and algebraic/differential variable classification before trying more dependency changes.
  runtime: Docker `mambaorg/micromamba:1.5.10`, Python 3.8, `linux/amd64`
  dependencies: `cantera` 2.6.0; `assimulo`; `numpy<1.24`; `scipy`; `pandas`; `matplotlib`
  notes: Environment can be built, but IDA fails at `t=0`.

- slug: p2d-solver-hanrach
  status: unreproduced
  language: Python
  environment: Docker `python:3.11-slim` on `linux/arm64`
  primary_blocker: final original-grid README `run_main.py` attempt exits 137 after slow JAX compile/Jacobian
  likely_next_step: Add a documented reduced-grid smoke test or refactor JAX compilation/memory usage before full-size reproduction.
  runtime: Docker `python:3.11-slim`, Python 3.11.15, `linux/arm64`
  dependencies: `jax[cpu]`; `scipy`; `matplotlib`; compatibility shims for newer JAX APIs
  notes: Full original-grid run exits 137 after heavy JAX compile/Jacobian work.

- slug: p2d-model-dkong8s93
  status: unreproduced
  language: MATLAB
  environment: MATLAB R2021b
  primary_blocker: final alternate variant also fails at initial nearly singular matrix solve
  likely_next_step: Check initial conditions, boundary equations, and matrix assembly before treating it as an environment problem.
  runtime: MATLAB R2021b; Octave 11.1.0 also checked
  dependencies: No pinned dependency set found upstream
  notes: Fails during initial nearly singular matrix solve, so this appears code/model related.

- slug: pseudo-sim-liuyang12
  status: success
  language: MATLAB
  environment: MATLAB R2021b plus TOMLAB/fmincon compatibility shim
  primary_blocker: upstream requires TOMLAB/KNITRO; `fmincon` compatibility shim runs `T1master` to completion
  likely_next_step: document that the shim is a workaround rather than KNITRO-equivalent validation
  runtime: MATLAB R2021b
  dependencies: MATLAB Optimization Toolbox; TOMLAB/KNITRO-to-`fmincon` compatibility shim
  notes: Upstream expects TOMLAB/KNITRO-style APIs.

- slug: jubat
  status: success
  language: Julia
  environment: Julia 1.12.6
  primary_blocker: `ChooseCell` include-path/world-age patch required
  likely_next_step: upstream the include-path patch or document it as a reproduction patch
  runtime: Julia 1.12.6
  dependencies: No external Julia packages declared in the checked manifest; uses project source files and Julia stdlib
  notes: Required `ChooseCell` include-path/world-age patch.

- slug: batp2dfoam
  status: unreproduced
  language: C++ / OpenFOAM
  environment: Docker OpenFOAM 10 on `linux/amd64`
  primary_blocker: compiles and starts the test case, but remains in repeated PIMPLE iterations without completing or writing time directories
  likely_next_step: Reduce or repair the test case first; verify `setFields` dictionaries and solver stopping criteria.
  runtime: Docker `openfoam/openfoam10-paraview56`, OpenFOAM 10, `linux/amd64`
  dependencies: OpenFOAM 10 toolchain; g++ 9.4.0
  notes: Build succeeds and solver starts, but test case does not finish.

- slug: battsimpy
  status: success
  language: Python
  environment: Docker `micromamba` Python 2.7 on `linux/amd64` plus Debian `libx11-6`
  environment_file: REPRODUCTIONS/environments/battsimpy/Dockerfile
  primary_blocker: none
  likely_next_step: none
  runtime: Docker `mambaorg/micromamba:1.5.10`, Python 2.7, `linux/amd64`
  dependencies: `assimulo` 2.9; legacy `numpy`/`scipy`/`matplotlib`; Debian `libx11-6`; `MPLBACKEND=Agg`
  notes: Needs a legacy container; do not try to modernize first.

- slug: spectral-li-ion-spm
  status: success
  language: MATLAB
  environment: MATLAB R2021b plus `chebdif` compatibility shim
  primary_blocker: none
  likely_next_step: none
  runtime: MATLAB R2021b
  dependencies: `chebdif.m` compatibility shim
  notes: Small educational MATLAB example.

- slug: spme-oed
  status: success
  language: Python
  environment: Python 3.11 environment from recipe
  environment_file: REPRODUCTIONS/environments/spme-oed/environment.yml
  primary_blocker: none
  likely_next_step: none
  runtime: Python 3.11.15 environment from recipe
  dependencies: `pybamm` 26.4.1; `numpy` 2.4.4; `scipy` 1.17.1; `matplotlib` 3.10.9; `pandas` 3.0.2; `casadi` 3.7.2; `bayesian-optimization` 3.2.1; `scikit-learn` 1.8.0
  notes: Reuses the PyBaMM environment.

- slug: slide
  status: success
  language: C++ + MATLAB
  environment: CMake/Ninja build on macOS with AppleClang
  environment_file: REPRODUCTIONS/environments/slide/environment.yml
  primary_blocker: none
  likely_next_step: none
  runtime: macOS, AppleClang 17.0.0, CMake 4.3.2, Ninja 1.13.2
  dependencies: System C++20 compiler; conda build environment with `cmake` and `ninja`; MATLAB not required for C++ unit-test smoke reproduction
  notes: Built upstream commit `847fec7aaeaefb916c51e5264aa8aa0dcfe72b20`; CTest passed 8/8 unit tests in 64.62 s; default `slide` binary launched successfully.

- slug: lionsimba
  status: unreproduced
  language: MATLAB / Octave
  environment: MATLAB R2021b and Octave 11.1.0
  primary_blocker: final MATLAB check shows `IDAInit=0`, `IDASolve=0`, `IDAFree=0`, `casadi=0`
  likely_next_step: Provide pinned SUNDIALS/CasADi MATLAB install instructions or a container with those bindings prebuilt.
  runtime: MATLAB R2021b; Octave 11.1.0
  dependencies: SUNDIALS MATLAB interface expected; CasADi expected for some paths
  notes: Final check shows `IDAInit=0`, `IDASolve=0`, `IDAFree=0`, `casadi=0`.

- slug: mpet
  status: success
  language: Python
  environment: Docker `python:3.12-bookworm` on `linux/amd64`
  environment_file: REPRODUCTIONS/environments/mpet/Dockerfile
  primary_blocker: none
  likely_next_step: none
  runtime: Docker `python:3.12-bookworm`, Python 3.12.13, `linux/amd64`
  dependencies: `daetools` 2.3.0; `PyQt5` 5.15.11; `numpy` 2.4.4; `scipy` 1.17.1; `matplotlib` 3.10.9; `pandas` 3.0.2; `h5py` 3.16.0; `schema` 0.7.8; `pytest` 9.0.3; Debian `libgfortran5`; `libgl1-mesa-glx`
  notes: Docker is the cleanest path; native macOS install is less predictable.

- slug: petlion-jl
  status: success
  language: Julia
  environment: Julia 1.12.6
  environment_file: REPRODUCTIONS/environments/petlion-jl/Project.toml
  primary_blocker: none
  likely_next_step: none
  runtime: Julia 1.12.6
  dependencies: `PETLION` 1.0.6; `Sundials` 4.28.0; `SciMLBase` 2.99.0; `Symbolics` 6.58.0; `SparseDiffTools` 2.26.0; `KLU` 0.6.0; `Dierckx` 0.5.4; `ForwardDiff` 1.3.3; `StaticArrays` 1.9.18
  notes: Clean Julia reproduction.

- slug: dfn-scott-moura
  status: success
  language: MATLAB
  environment: Octave 11.1.0
  environment_file: REPRODUCTIONS/environments/octave/Dockerfile
  primary_blocker: `dfn_federico` completes; `dfn_rg` is not the working Octave entry in this snapshot
  likely_next_step: document `dfn_federico` as the preferred Octave smoke entry
  runtime: Octave 11.1.0
  dependencies: No extra Octave packages required in the successful path
  notes: `dfn_federico` completes; `dfn_rg` is not the working Octave entry in this snapshot.

- slug: fastdfn
  status: success
  language: MATLAB
  environment: Octave 11.1.0
  environment_file: REPRODUCTIONS/environments/octave/Dockerfile
  primary_blocker: none
  likely_next_step: none
  runtime: Octave 11.1.0
  dependencies: No extra Octave packages required in the successful path
  notes: Fast educational DFN path.

- slug: spmet
  status: success
  language: MATLAB
  environment: Octave 11.1.0
  environment_file: REPRODUCTIONS/environments/octave/Dockerfile
  primary_blocker: required `mkoctfile --mex lininterp1f.c` first
  likely_next_step: document the MEX helper build before the first run
  runtime: Octave 11.1.0
  dependencies: `mkoctfile` for `lininterp1f.c` MEX build
  notes: Build the MEX helper before running.
